import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { DocumentStoreCreator } from "../DocumentStoreCreator";
export declare class DocumentStoreCreator__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<DocumentStoreCreator>;
    getDeployTransaction(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): DocumentStoreCreator;
    connect(signer: Signer): DocumentStoreCreator__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): DocumentStoreCreator;
}
