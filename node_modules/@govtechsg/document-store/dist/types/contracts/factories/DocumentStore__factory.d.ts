import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { DocumentStore } from "../DocumentStore";
export declare class DocumentStore__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(_name: string, overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<DocumentStore>;
    getDeployTransaction(_name: string, overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): DocumentStore;
    connect(signer: Signer): DocumentStore__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): DocumentStore;
}
