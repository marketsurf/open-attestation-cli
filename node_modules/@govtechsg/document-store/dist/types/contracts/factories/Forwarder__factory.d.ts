import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { Forwarder } from "../Forwarder";
export declare class Forwarder__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<Forwarder>;
    getDeployTransaction(overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): Forwarder;
    connect(signer: Signer): Forwarder__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): Forwarder;
}
