import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { NaivePaymaster } from "../NaivePaymaster";
export declare class NaivePaymaster__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(_name: string, overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<NaivePaymaster>;
    getDeployTransaction(_name: string, overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): NaivePaymaster;
    connect(signer: Signer): NaivePaymaster__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): NaivePaymaster;
}
