import { Signer, ContractFactory, Overrides } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import type { OwnableDocumentStore } from "../OwnableDocumentStore";
export declare class OwnableDocumentStore__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(_name: string, overrides?: Overrides & {
        from?: string | Promise<string>;
    }): Promise<OwnableDocumentStore>;
    getDeployTransaction(_name: string, overrides?: Overrides & {
        from?: string | Promise<string>;
    }): TransactionRequest;
    attach(address: string): OwnableDocumentStore;
    connect(signer: Signer): OwnableDocumentStore__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): OwnableDocumentStore;
}
