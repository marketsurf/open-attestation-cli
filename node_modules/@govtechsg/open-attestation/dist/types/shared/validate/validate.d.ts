import { ErrorObject, ValidateFunction } from "ajv";
import { Kind } from "../utils/@types/diagnose";
export declare const validateSchema: (document: any, validator: ValidateFunction, kind?: Kind | undefined) => ErrorObject[];
