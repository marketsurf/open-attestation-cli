import { Signer } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import { ContractFactory, Overrides } from "@ethersproject/contracts";
import type { TitleEscrowCreator } from "../TitleEscrowCreator";
export declare class TitleEscrowCreator__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(overrides?: Overrides): Promise<TitleEscrowCreator>;
    getDeployTransaction(overrides?: Overrides): TransactionRequest;
    attach(address: string): TitleEscrowCreator;
    connect(signer: Signer): TitleEscrowCreator__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): TitleEscrowCreator;
}
