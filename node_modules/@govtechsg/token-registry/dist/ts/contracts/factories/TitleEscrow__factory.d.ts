import { Signer } from "ethers";
import { Provider, TransactionRequest } from "@ethersproject/providers";
import { ContractFactory, Overrides } from "@ethersproject/contracts";
import type { TitleEscrow } from "../TitleEscrow";
export declare class TitleEscrow__factory extends ContractFactory {
    constructor(signer?: Signer);
    deploy(_tokenRegistry: string, _beneficiary: string, _holder: string, _titleEscrowFactoryAddress: string, overrides?: Overrides): Promise<TitleEscrow>;
    getDeployTransaction(_tokenRegistry: string, _beneficiary: string, _holder: string, _titleEscrowFactoryAddress: string, overrides?: Overrides): TransactionRequest;
    attach(address: string): TitleEscrow;
    connect(signer: Signer): TitleEscrow__factory;
    static connect(address: string, signerOrProvider: Signer | Provider): TitleEscrow;
}
